load("@crate_index//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_doc_test", "rust_library", "rust_proc_macro")

rust_proc_macro(
    name = "mail-uniffi-macros",
    srcs = glob(["src/**/*.rs"]),
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(
        normal = True,
    ),
)

rust_doc_test(
    name = "mail-uniffi-macros-doc-test",
    size = "small",
    crate = ":mail-uniffi-macros",
)
