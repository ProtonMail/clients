load("@crate_index//:defs.bzl", "all_crate_deps")
load("@rules_rust//cargo:defs.bzl", "cargo_build_script")
load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

cargo_build_script(
    name = "build_script",
    srcs = ["build.rs"],
    data = ["data/windowsZones.xml"],
    deps = all_crate_deps(
        build = True,
    ),
)

rust_library(
    name = "mail-ical-tz",
    srcs = glob(["src/**/*.rs"]),
    compile_data = ["Cargo.toml"],
    edition = "2024",
    proc_macro_deps = all_crate_deps(
        proc_macro = True,
    ),
    visibility = ["//visibility:public"],
    deps = all_crate_deps(
        normal = True,
    ) + [
        ":build_script",
    ],
)

rust_test(
    name = "mail-ical-tz-test",
    size = "small",
    crate = ":mail-ical-tz",
    deps = all_crate_deps(normal_dev = True),
)
